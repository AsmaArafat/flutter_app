import 'package:flutter_app/services/services.dart';
import 'package:flutter_app/models/login_request.dart';
import 'dart:convert';

class LoginServices {
//  Future<bool> performLogin(String username, String password) async {
//    try {
//      LoginRequest loginRequest =
//          LoginRequest(username: username, password: password);
//      String requestBody = jsonEncode(loginRequest);
//      print("Request");
//      print(requestBody);
//      Map<String, dynamic> responseJson =
//          await Services().post("login", requestBody);
//      print("Login token: ${responseJson['token']}");
//      return true;
//    } catch (e) {
//      throw e;
//    }
//  }
}
